{
	"schemaVersion": 1,
	"id": "autoclicker",
	"version": "${version}",
	"name": "Autoclicker",
	"description": "Hold a key, click 10 times per second - instantly, from the first tick, in the world and inside GUIs. Separate binds for left (attack) and right (use), both configurable in Options -> Controls -> Key Binds.",
	"authors": [
		"Tima"
	],
	"license": "MIT",
	"environment": "client",
	"entrypoints": {
		"client": [
			"dev.autoclicker.AutoClickerClient"
		]
	},
	"mixins": [
		{
			"config": "autoclicker.client.mixins.json",
			"environment": "client"
		}
	],
	"depends": {
		"fabricloader": ">=0.19.0",
		"minecraft": "~1.21.11",
		"java": ">=21",
		"fabric-api": "*"
	}
}
